<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a123">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.123">
        <activity android:name=".UserStaticInfo"></activity>
        <activity android:name=".UserActivity" />
        <activity android:name=".User" />
        <activity android:name=".Transform" />
        <activity android:name=".SignActivity" />
        <activity android:name=".ProfileMapsActivity" />
        <activity android:name=".LoadedUserDataActivity" />
        <activity android:name=".AnimImageFragment" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>